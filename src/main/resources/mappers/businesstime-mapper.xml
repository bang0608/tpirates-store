<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tpirates.store.dao.BusinessTimeDao">
	
	<insert id="insertAll" parameterType="java.util.List">
		INSERT INTO business_time (store_id, day, open, close) VALUES
		<foreach collection="saveDtoList" item="item" separator=" , " >
		    ("${item.id}", 
		    
		    (CASE ${item.day} WHEN 'Monday' THEN 1),
		    (CASE ${item.day} WHEN 'Tuesday' THEN 2),
		    (CASE ${item.day} WHEN 'Wednesday' THEN 3),
		    (CASE ${item.day} WHEN 'Thursday' THEN 4),
		    (CASE ${item.day} WHEN 'Friday' THEN 5),
   
		    "${item.open}", "${item.close}")
		</foreach>
	</insert>
</mapper>